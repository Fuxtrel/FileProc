#include "file_proc.h"
using namespace std;





int main() {
   cout << "Start processing, input the command\n";
   string command;
   getline(cin, command);
   string str = deleteSpace(command);
   for(int i = 0; str[i] != ' '; i++){
       if(str[i] == '.'){

       }
   }



}
